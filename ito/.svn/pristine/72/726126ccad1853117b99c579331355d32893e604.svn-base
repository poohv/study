<!DOCTYPE html>
<html layout:decorator="thymeleaf/layout/layout_default">
<head>
<title layout:title-pattern="$LAYOUT_TITLE : $CONTENT_TITLE">메인</title>
</head>
<body>

	<div layout:fragment="content">
		<div class="easyui-tabs" style="height: auto;"></div>
	</div>

	<th:block layout:fragment="script">
		<script>
			var tabsComp;
			
			$(function() {
				var menuId = '[(${menu_id})]';
				
				tabsComp = $('.easyui-tabs');
				
				/* tabsComp.createTabs({
					id: 'home',
					title: '[(#{title.home})]',
					//isIframe: true,
					//url: _ctx + '/adminlte/pages/UI/general.html',
					content: '홈',
					method: 'add',
					closable: false
				}); */
				
			    $('.nav-sidebar .nav-link[data-menu-id]').on('click', function() {
			    	var tabTitle = $(this).text();
					var tabId = $(this).data('menu-id');
					var tabContentUrl = $(this).data('menu-url');
			    	var closeable = true;
			    	
			    	if('contract' == tabId) {
			    		closeable = false;
			    	}
			    	
			    	// 선택 메뉴 하이라이트
			    	$('.nav-sidebar .nav-link').removeClass('active');
			    	$(this).addClass('active');
			    	$(this).parent().parent().prev('.nav-link').addClass('active menu-open');
			    	$(this).parent().parent().prev('.nav-link').parent().addClass('menu-open');
			    	
					tabsComp.createTabs({
						id: tabId,
						title: tabTitle,
						isIframe: true,
						url: _ctx + tabContentUrl,
						method: 'add',
						closable: closeable
					});
				});
			    
			    $('.nav-sidebar .nav-link[data-menu-id="contract"]').click();
			    
			    if(menuId) {
			    	$('.nav-sidebar .nav-link[data-menu-id="' + menuId + '"]').click();
			    }
			});
		</script>
	</th:block>
</body>
</html>